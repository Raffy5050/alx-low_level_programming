#include <stdio.h>

/**
*main - function is the entry point that returns integer value
*Return: return 0 to whomever called it as a success
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
