# 0x07. C - Even more pointers, arrays and strings
- Allowed editors: vi, vim, emacs
- All files are compiled using Ubuntu 20.04 LTS using gcc, using the options -Wall -Werror -Wextra -pedantic -std=gnu89
- Use of functions like printf, puts, etcâc. was not used
